{
  "name": "wss2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run build && npm run test:progress",
    "test:progress": "node tests/progress.test.js"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  }
}
