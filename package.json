{
  "name": "wss2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm exec -- vite build",
    "preview": "vite preview",
    "test": "npm exec -- vite build"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
